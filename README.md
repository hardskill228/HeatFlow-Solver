# 🌡️ HeatFlow Solver — огляд і відповідність вимогам курсової

Цей README відображає поточний стан реалізації вимог курсової, зміни, які я застосував, а також кроки для повної відповідності (включно з масштабуванням і чергою задач).

Коротко: проект уже реалізує ключові механізми — валідацію складності задач, WebSocket оновлення прогресу, збереження результатів у `backend/data/accounts.json` та можливість скасування. Я додав `nginx` конфіг для балансування HTTP та WebSocket між двома API-інстансами і базові Dockerfile, щоб `docker-compose` можна було підняти локально.

## Чи відповідає проект вимогам?

Нижче — покрокова відповідь по кожному пункту вимог:

1) Обмежена максимальна трудомісткість — PASS
   - У `backend/server.py` є перевірка `MAX_NODES` і додаткова валідація `iterations`. Запит, що перевищує обмеження, отримає помилку 400.

2) Клієнт інформується про хід виконання (прогрес/етап) — PASS
   - Є WebSocket `/ws/{task_id}`, який надсилає повідомлення зі статусом, етапом і прогресом. Фронтенд підписується й відображає оновлення.

3) Історія результатів, стан поточної задачі, скасування, запуск нової задачі — PARTIAL
   - Історія завершених задач зберігається у `backend/data/accounts.json` (для зареєстрованих користувачів).
   - Поточні стани та прапори pause/cancel зберігаються в пам'яті кожного інстансу. Це означає, що при запуску кількох інстансів стан НЕ є централізованим. Щоб повністю відповідати вимозі (особливо у багатосерверній архітектурі), треба додати централізоване сховище станів (Postgres/Redis).

4) Авторизація клієнта (HTTPS + безпечне хешування) — PARTIAL
   - Реалізовано простий username/password flow з хешуванням SHA-256 і токеном у пам'яті. Для повної безпеки необхідне HTTPS за допомогою Nginx (Let's Encrypt) та заміна SHA-256 на `bcrypt`/`argon2` і, бажано, JWT.

5) Балансування навантаження мінімум для 2 серверів — NOW SUPPORTED FOR HTTP/WS ROUTING
   - Додав `nginx/nginx.conf`, а також `backend/Dockerfile.api` і `backend/Dockerfile.worker`. `docker-compose.yml` уже мав визначення `nginx`, `api1`, `api2`.
   - Після запуску `docker-compose up --build` Nginx проксуватиме HTTP та WebSocket трафік між `api1` та `api2`.
   - Заувага: поточне виконання задач відбувається локально в тій API-інстанції, яка прийняла POST `/api/tasks/start` (тобто балансування виконує розподіл запитів між two app servers). Для більш правильної архітектури рекомендується віддати виконання завдань worker-процесам через Celery/Redis.

## Що я зробив зараз (зміни в репозиторії)

- Додав `nginx/nginx.conf` для балансування та WebSocket-проксі.
- Додав `backend/Dockerfile.api` та `backend/Dockerfile.worker` щоб `docker-compose` будував образи без помилок.

## Як перевірити балансування локально (PowerShell)

```powershell
cd C:\Users\Pavlo\Desktop\heatflow-solver
docker-compose up --build
```

Відкрийте `http://localhost` — Nginx проксуватиме запити до API. Відправляйте кілька паралельних запитів `/api/tasks/start` — вони будуть розподілені між `api1` та `api2`.

## Головні кроки, які залишилось зробити (щоб повністю відповідати всім пунктам та додатковим балам)

1) Інтеграція Celery + Redis (важливо)
   - API ставить задачі в чергу, worker'и виконують їх. Дає справжнє розподілення обчислень та дозволяє масштабувати worker'и незалежно від API.

2) Redis Pub/Sub або інша система повідомлень для ретрансляції прогресу
   - Worker публікує прогрес у Redis; API-сервер підписується і ретранслює повідомлення через WebSocket клієнтам.

3) Персистентність стану задач
   - Використати Postgres для зберігання метаданих задач, стану, черги історії; це полегшить моніторинг та адмін-панель.

4) HTTPS і безпечне зберігання паролів
   - Налаштувати TLS у Nginx (Let's Encrypt), замінити SHA-256 на bcrypt/argon2, додати JWT для авторизації.

5) Поведінка при вичерпанні ресурсів (пункт 6)
   - Celery + Redis дозволяють реалізувати чергу; фронтенд може показувати позицію у черзі і приблизний час очікування.

6) Автоскейлінг (пункт 7)
   - Використати Docker Swarm/Kubernetes або скрипти/інструменти для автоматичного додавання worker-реплік у відповідь на довжину черги.

7) Адмін-панель (пункт 8)
   - Реалізувати `/admin` інтерфейс, що читає з Postgres/Redis інформацію про поточні задачі, чергу та статистику.

## Пропозиція дій — що зроблю наступним кроком, якщо ви дозволите

1. Реалізувати Celery scaffold + Redis pub/sub і оновити API так, щоб задачі виконувалися worker'ами, а прогрес публікувався в Redis.
2. Додати приклад реалізації публікації прогресу з worker та підписки у API для ретрансляції WS.
3. Замінити SHA-256 на bcrypt та додати приклад конфігурації TLS для Nginx.

Напишіть будь ласка, який варіант ви обираєте:

- "Зроби Celery/Redis" — я реалізую повноцінний розподіл задач та pub/sub для WS.
- "Лише документація" — я лише додам детальну інструкцію та сценарії тестування.
- "Інше" — пропонуйте конкретні наступні зміни.
# 🌡️ HeatFlow Solver: Сервіс Асинхронних Обчислень МСЕ

## 📌 Огляд Проєкту

**HeatFlow Solver** — це веб-застосунок для демонстрації архітектури, здатної ефективно керувати та виконувати **трудомісткі наукові задачі** (симуляція теплопередачі за допомогою Методу Скінченних Елементів, МСЕ) в одному, але високоефективному **асинхронному** Python-процесі.

Архітектура використовує **FastAPI** та **`asyncio`** для:

  * Прийому запитів та негайного повернення відповіді.
  * Виконання обчислень **у фоновому потоці** (`asyncio.to_thread`).
  * Надання **зворотного зв'язку клієнту в реальному часі** через WebSockets.

-----

## ✅ Реалізовані Вимоги

| № | Вимога | Реалізація |
| :--- | :--- | :--- |
| **1.** | Обмеження трудомісткості | Валідація ліміту на максимальну кількість **вузлів сітки** (100,000) на рівні API. |
| **2.** | Інформування про хід виконання | Використання **WebSockets** для оновлення прогресу (у відсотках та етапах) у реальному часі. |
| **3.** | Керування задачами | Зберігання стану та історії завдань у **локальному словнику Python** (in-memory storage), можливість запуску та **скасування** активних завдань. |
| **4.** | Авторизація | **(Мок)** Залишено місце для реалізації JWT/HTTPS. Логіка застосунку доступна локально. |
| **5.** | **Балансування навантаження** | **Імітація розподілу:** Використовується **асинхронний пул потоків** (`asyncio.to_thread`) для паралельного виконання декількох обчислювальних завдань на єдиному сервері. |

-----

## 🛠️ Технологічний Стек

### Backend & Обчислення (Єдиний Сервіс)

  * **Python:** Основна мова розробки.
  * **FastAPI:** Ядро API-сервера, що забезпечує асинхронність та керування WebSockets.
  * **`asyncio` + `ThreadPoolExecutor`:** Використовується для запуску трудомістких обчислень у фоновому потоці, не блокуючи основний цикл обробки HTTP-запитів.
  * **Сховище:** Локальний словник Python **(In-Memory)** для зберігання історії та поточного стану завдань.
  * **NumPy:** Бібліотека для виконання числових обчислень та імітації МСЕ.

### Frontend

  * **HTML, CSS, JavaScript:** Простий, самодостатній клієнт, який взаємодіє з API та WebSockets.

-----

## ⚙️ Розгортання та Запуск (Локально)

Для запуску проєкту необхідні лише **Python 3.11+** та **віртуальне середовище (venv)**.

### 1\. Підготовка Середовища

Переконайтеся, що ви знаходитесь у кореневій папці проєкту (`heatflow-solver/`).

```bash
# 1. Створення та активація віртуального середовища
python -m venv venv
.\venv\Scripts\Activate # Активація в Windows PowerShell

# 2. Встановлення залежностей
pip install -r backend/requirements.txt
```

### 2\. Запуск Сервера

Використовуйте `uvicorn` для запуску асинхронного сервера.

```bash
# Запуск сервера у режимі розробки
uvicorn backend.server:app --reload
```

### 3\. Доступ до Застосунку

Відкрийте браузер за адресою:

[http://127.0.0.1:8000](https://www.google.com/search?q=http://127.0.0.1:8000)

-----

## 📂 Структура Проєкту

```
heatflow-solver/
├── .gitignore
├── README.md
├── LICENSE
├── backend/
│   ├── requirements.txt
│   └── server.py        <-- Ядро всієї системи (API, Worker, DB)
└── frontend/
    └── index.html       <-- Інтерфейс користувача
```